name: Create and publish a Docker image

on:
  push:
    branches: ['main']

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: PerfAI Actions
        uses: PerfAI-Inc/PerfAI-Action@v1.8.1
        with:
          apiSpecURL: 'https://api-dev.wehostvoip.io/openapi.json'
          apiBasePath: 'https://api.wehostvoip.io'
          authUrl: 'https://api.wehostvoip.io/v1/login'
          authBody: '"{"email":"tester@daniyals.ca","password":"Tester@786"}"'
          authHeaders: ''
          licenseKey: 'your-license-key'
      

        
