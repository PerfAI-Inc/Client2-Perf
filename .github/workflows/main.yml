name: Create and publish a Docker image

on:
  push:
    branches: ['main']

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: PerfAI Actions
        uses: PerfAI-Inc/PerfAI-Action@v1.8.1
        with:
          apiSpecURL: 'https://api.fortniteapi.io/openapi.json'
          apiBasePath: 'https://api.fortniteapi.io'
          authUrl: ''
          authBody: ''
          authHeaders: 'API Key: 0ff5d063-0ca4acd0-803bea84-957f9e21'
          licenseKey: 'your-license-key'
      

        
